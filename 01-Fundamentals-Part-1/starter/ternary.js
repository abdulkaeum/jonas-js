// is an expression

const age = 23;

age >= 17 ? console.log('x') : console.log('y');

let value = 'a';
value = age >= 17 ? 'x' : 'y';
console.log(value);

console.log(`THE LETTER IS ${age >= 17 ? 'x' : 'y'}`);